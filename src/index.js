import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // この部分が正しいか確認
import './index.css';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
